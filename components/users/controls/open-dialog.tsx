"use client"

import { FC } from "react";

export const OpenDialog: FC = () => {
    return (
        <span
            className="px-8 py-2 font-medium text-white bg-[#d19b36] rounded-xl"
            onClick={() => {}}
        >
            Nuevo
        </span>
    );
};

export default OpenDialog;